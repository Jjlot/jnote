# Refs:https://linoxide.com/linux-how-to/install-configure-utorrent-linux/

curl -O http://download-hr.utorrent.com/track/beta/endpoint/utserver/os/linux-x64-ubuntu-13-04
tar zxf utserver.tar.gz -C /opt/
cd /opt/utorrent-server*
unzip webui.zip
ldd utserver | egrep 'not found'

# Install libssl
ln -s /lib64/libssl.so.1.0.2k /lib64/libssl.so.1.0.0

# Install libcrypto
curl -O https://www.openssl.org/source/openssl-1.0.0r.tar.gz
cd openssl-1.0.0r
./config shared && make
cp libssl.so.1.0.0 libcrypto.so.1.0.0 /lib64

# Start
./utserver -settingspath /home/utorrent

# Access
http://localhost:8080/gui/
The username is admin and leave the password field empty. 
